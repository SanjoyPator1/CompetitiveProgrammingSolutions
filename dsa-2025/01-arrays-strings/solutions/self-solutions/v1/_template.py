"""
╔══════════════════════════════════════════════════════════════╗
║              DSA 2025 — Solution Template                    ║
║              Language: Python 3                              ║
╚══════════════════════════════════════════════════════════════╝

Usage:
    1. Copy this template for each new problem
    2. Fill in the metadata at the top
    3. Implement brute force FIRST, then optimize
    4. Run: python3 <filename>.py

Pattern Quick Reference:
    🔀 Two Pointers    — sorted arrays, palindromes, pair finding
    🪟 Sliding Window  — substrings/subarrays with constraints
    #️⃣ Hash Map/Set    — frequency, O(1) lookup, grouping
    📊 Prefix/Suffix   — running aggregates, product problems
    🔧 In-Place        — space-constrained modifications
"""

# ─── Problem Metadata ───────────────────────────────────────
# Problem:    [Name]
# Difficulty: [Easy / Medium / Hard]
# Pattern:    [Two Pointers / Hash Map / Sliding Window / Prefix-Suffix / In-Place]
# LeetCode:   [URL if applicable]
# Key Insight: [One-liner about the core trick]
# Time:  O(?)  →  O(?)  (brute → optimized)
# Space: O(?)  →  O(?)  (brute → optimized)
# ─────────────────────────────────────────────────────────────


from typing import List, Optional


def solve_brute_force():
    """
    Brute force approach — ALWAYS start here.
    
    Think out loud:
    - What's the most obvious way to solve this?
    - What's the time/space complexity?
    - Why is this not efficient enough?
    """
    pass


def solve_optimized():
    """
    Optimized approach using [PATTERN NAME].
    
    Think out loud:
    - Which pattern applies here and why?
    - What data structure helps?
    - Walk through an example step-by-step.
    """
    pass


# ─── Test Cases ──────────────────────────────────────────────
if __name__ == "__main__":
    # ✅ Basic test cases
    # assert solve_optimized(...) == expected

    # 🔪 Edge cases
    # assert solve_optimized(...) == expected  # empty input
    # assert solve_optimized(...) == expected  # single element
    # assert solve_optimized(...) == expected  # all same elements

    print("✅ All tests passed!")
